nelmio_cors:
    defaults:
        # for security reasons we do not allow CORS by default
        allow_origin: []
        allow_headers: []
        max_age: 0
    paths:
        '^/api/':
            allow_origin: ['*']
            allow_headers: ['Content-Type', 'Authorization']
            max_age: 3600

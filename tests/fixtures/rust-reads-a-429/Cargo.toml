[package]
name = "registry"
version = "1.0.0"
edition = "2021"

[dependencies]
axum = "0.7"
http = "1"
diesel = { version = "2.2", features = ["postgres"] }
tokio = { version = "1", features = ["full"] }

nelmio_cors:
    defaults:
        allow_origin: []
        allow_headers: []
        max_age: 0
    paths:
        '^/api/':
            allow_origin: ['https://app.acme.com']
            allow_headers: ['Content-Type', 'Authorization']
            max_age: 3600

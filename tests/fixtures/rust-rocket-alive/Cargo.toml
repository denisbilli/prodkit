[package]
name = "vault"
version = "1.0.0"
edition = "2021"

[dependencies]
rocket = { version = "0.5.1", features = ["json"] }
governor = "0.10.4"
diesel = { version = "2.2", features = ["postgres"] }
serde_json = "1.0"

services:
  api:
    build: .
    ports:
      - "3000:3000"
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: local
  cache:
    image: redis:7-alpine
  worker:
    build: .
    command: node src/worker.js
